# NatureNuclideLibrary — 정적 웹 애플리케이션 상세 설계서

## 1. 목적

`NatureNuclideLibrary`는 원본 Excel 파일 **「검출기별_측정에너지_핵종판독_Model.xlsx」**의 내용을 GitHub Pages에서 조회·탐색·비교·계산할 수 있도록 변환한 정적 웹 애플리케이션이다.

핵심 질문은 다음과 같다.

> “이 에너지의 피크는 어떤 핵종 후보이며, 어디에서 발생했고, 어떤 화학형으로 이동했으며, 어떤 시료에서 어떤 검출기로 관측할 수 있는가?”

단순히 Excel 표를 웹에 복제하는 대신, 사용자의 실제 판독 흐름에 맞춰 **에너지 → 핵종 후보 → 발생원 → 화학형 → 시료 → 검출기 적합성 → 간섭/분해능 확인** 순으로 탐색하도록 설계한다.

---

## 2. 설계 원칙

1. **원본 보존성**: Excel 11개 시트의 값과 계산 결과를 모두 웹에서 확인할 수 있어야 한다.
2. **판독 중심 UI**: 사용자가 에너지 값을 입력하면 가장 가까운 핵종 후보를 즉시 제시한다.
3. **다차원 필터**: 핵종, 에너지, 검출기, 발생원, 화학형, 시료를 조합해 필터링한다.
4. **계산 투명성**: FWHM 및 분해능 판정식을 UI에 명시하고 사용자가 계수를 조정할 수 있다.
5. **GitHub Pages 완전 호환**: 서버, 데이터베이스, 빌드 시스템 없이 동작한다.
6. **외부 의존성 최소화**: CDN이나 프레임워크 없이 순수 HTML/CSS/JavaScript로 구현한다.
7. **전문적 시각 언어**: 과도한 색상, 캐릭터, 장식보다 정보 밀도·가독성·계층을 우선한다.
8. **Light/Dark 지원**: 테마 선택을 저장해 다음 방문에도 유지한다.
9. **접근성**: 키보드 탐색, 포커스 표시, 대비, reduced-motion을 고려한다.
10. **연구·실무 보조 도구**: 결과를 최종 핵종 동정이나 MDA 판정으로 오인하지 않도록 주의문을 항상 노출한다.

---

## 3. 원본 데이터 구조

원본 Excel은 11개 시트로 구성된다.

| 시트 | 웹 기능 매핑 |
|---|---|
| `00_사용법·판정규칙` | 시작 안내, 판정 규칙 |
| `01_★라인마스터` | 핵심 감마선 라인 검색 및 필터 |
| `02_에너지대역×검출기` | 에너지 대역별 검출기 비교 |
| `03_검출기별_적소카드` | 검출기 카드/비교 |
| `04_비감마_핵종경로` | 순β·α 등 비감마 측정경로 |
| `05_발생원×화학형×시료` | 발생원–화학형–시료–검출기 사슬 |
| `06_화학형태_포집사전` | 포집 매체/불가 매체/QC 사전 |
| `07_에너지_간섭맵` | 인접 감마선 간섭 및 분해능 판정 |
| `08_FWHM·분해능_계산기` | 사용자 입력 계산기 |
| `09_시료별_1순위검출기` | 시료별 권장 검출기 및 MDA 참고 |
| `10_출처·주의` | 출처, 모델 가정, 사용상 주의 |

웹 배포본의 `assets/data.js`에는 각 시트의 **계산된 값과 원본 Excel 수식**을 함께 포함한다. 따라서 “원본 시트 보기” 화면에서 수식 표시를 켜면 Excel 계산 논리를 추적할 수 있다.

---

## 4. 정보구조

### 4.1 좌측 내비게이션

- 개요 대시보드
- 빠른 에너지 판독
- 감마 라인 마스터
- 검출기 비교
- 발생원·시료 경로
- 비감마 핵종 경로
- 화학형 포집사전
- 간섭 분석
- FWHM 계산기
- 원본 시트 브라우저
- 출처·주의

### 4.2 상단 공통 영역

- 현재 화면 제목
- 전체 검색
- Light/Dark 테마 토글
- 모바일 메뉴 버튼

### 4.3 우측/본문 공통 패턴

- KPI 카드
- 필터 바
- 결과 수 표시
- 정렬 가능한 데이터 표
- 결과 CSV 내보내기
- 즐겨찾기 저장
- 행 상세 패널

---

## 5. 핵심 사용자 시나리오

### 시나리오 A — “661.7 keV 피크가 무엇인가?”

1. 빠른 에너지 판독에서 `661.7` 입력
2. 허용 오차(예: ±2 keV) 지정
3. 필요 시 `NaI(Tl)` 또는 `HPGe` 검출기 선택
4. 거리순으로 후보 라인 정렬
5. 핵종, 반감기, 방출확률, 발생원, 화학형, 시료, 검출기 적합성, 간섭 주의를 한 화면에서 확인
6. 필요한 라인을 즐겨찾기에 저장

### 시나리오 B — “NaI로 볼 수 있는 핵종만 보고 싶다”

1. 감마 라인 마스터로 이동
2. 검출기 필터를 `NaI(Tl)`로 선택
3. 적합성 기준을 `●만` 또는 `●+◐`로 선택
4. 에너지/핵종/시료 검색 추가
5. 현재 결과를 CSV로 저장

### 시나리오 C — “공기 중 요오드를 어떤 매체와 검출기로 측정하는가?”

1. 발생원·시료 경로 또는 화학형 포집사전으로 이동
2. `요오드`, `I-131`, `CH3I`, `공기` 검색
3. TEDA 함침탄/AgX, 입자필터와의 역할 차이 확인
4. 후속 검출기와 QC 조건 확인

### 시나리오 D — “두 감마선이 NaI에서 분해되는가?”

1. 간섭 분석에서 두 에너지 또는 ΔE 입력
2. NaI의 662 keV 분해능 값을 조정
3. FWHM 및 `R = ΔE/FWHM` 계산
4. `R ≥ 2`, `1 ≤ R < 2`, `R < 1` 기준에 따라 상태 표시
5. HPGe/LaBr3와 결과 비교

---

## 6. 화면별 상세 기능

### 6.1 개요 대시보드

- 총 감마선 라인 수
- 고유 핵종 수
- 에너지 범위
- 검출기 유형 수
- 비감마 경로 수
- 시료 유형 수
- 검출기별 `●` 라인 수 막대 그래프
- 에너지 대역별 라인 수 막대 그래프
- 최근 즐겨찾기
- 즉시 판독 입력창

### 6.2 빠른 에너지 판독

입력:
- 관심 에너지 `E` (keV)
- 허용 오차 `±ΔE`
- 검출기
- `●만` 필터 여부

출력:
- `|Eline - Einput|` 거리
- 핵종
- Eγ
- pγ
- 반감기
- 발생원
- 생성경로
- 화학형태
- 시료·채취매체
- 선택 검출기 적합성
- TCS
- 간섭·주의

정렬 우선순위:
1. 에너지 거리 오름차순
2. 선택 검출기 적합성 `● > ◐ > ✕`
3. 방출확률 `pγ` 내림차순

### 6.3 감마 라인 마스터

필터:
- 전체 텍스트 검색
- 핵종
- 최소/최대 에너지
- 검출기
- 적합성 등급
- 에너지 대역

기능:
- 열 정렬
- 페이지 크기 조절
- CSV 저장
- 즐겨찾기
- 상세 행 보기

### 6.4 검출기 비교

Excel `03_검출기별_적소카드`를 카드 UI로 재구성한다.

각 카드:
- 실용 에너지창
- 분해능
- 상대 효율
- 운용 조건
- 대표 핵종
- 놓치는 핵종/조건
- 대표 시료
- 핵심 한계

비교 기능:
- 2~3개 검출기를 선택해 항목별 비교 가능

### 6.5 발생원·시료 경로

두 데이터셋을 한 화면에서 탭으로 제공한다.

- 발생원 → 화학형 → 시료 → 검출기 사슬 (`05`)
- 시료 → 1순위 검출기 → 보이는 핵종 (`09`)

검색어 하나로 발생원/핵종/화학형/시료/검출기를 동시에 검색한다.

### 6.6 비감마 핵종 경로

- H-3, C-14 등 감마선만으로 직접 측정하기 어려운 핵종을 별도 표시
- 방출 유형
- 대표 에너지
- 발생원
- 화학형
- 시료
- 전처리
- 최종 검출기

감마 라이브러리에 비감마 핵종을 넣어 “미검출”로 해석하는 오류를 방지하는 교육적 역할을 한다.

### 6.7 화학형 포집사전

화학형을 기준으로 다음을 한 줄에서 확인한다.

`화학형 → 대표핵종 → 포집원리 → 적합매체 → 부적합매체 → 검출기 → QC`

### 6.8 간섭 분석

#### 원본 간섭맵
Excel `07`의 계산 결과를 표로 표시한다.

#### 사용자 계산
HPGe:

`FWHM(E) = sqrt(a + bE)`

NaI / LaBr3:

`FWHM(E) = (R662/100) * sqrt(662E)`

분리 지표:

`R = ΔE / FWHM`

기본 기하 판정:
- `R ≥ 2`: 완전 분해
- `1 ≤ R < 2`: 부분 분해 / deconvolution 고려
- `0.5 ≤ R < 1`: 어깨 정도
- `< 0.5`: 실질적 분해 곤란

※ 이는 피크 강도비, 배경, 계수통계, tailing 등을 모두 포함하는 완전한 검출성 판정이 아니다.

### 6.9 FWHM 계산기

사용자 입력:
- 관심 에너지
- 이웃 선과의 ΔE
- 약한선/강한선 강도비 ρ
- HPGe a, b
- NaI R662
- LaBr3 R662

결과:
- FWHM
- 상대분해능
- R
- 기하 판정
- 강도비 보정 필요 R

### 6.10 원본 시트 브라우저

원본 Excel 11개 시트를 모두 제공한다.

기능:
- 시트 선택
- 전체 텍스트 검색
- 계산값/Excel 수식 전환
- 행/열 고정 헤더
- CSV 저장

이는 웹 전용 화면에서 생략된 정보를 원본 구조 그대로 확인하기 위한 “감사 가능성(auditability)” 화면이다.

---

## 7. 상태 및 저장

브라우저 `localStorage`:
- 테마
- 즐겨찾기 라인 ID
- 마지막 선택 검출기
- 테이블 페이지 크기

브라우저 `sessionStorage`:
- 현재 세션에서 비밀번호 확인 여부

개인정보나 서버 전송은 없다.

---

## 8. 비밀번호 접근 게이트

요구 비밀번호: `redpass`

정적 GitHub Pages에는 서버 세션과 비밀 저장소가 없으므로 진짜 접근제어를 구현할 수 없다.

구현 방식:
1. 비밀번호 입력값을 브라우저 Web Crypto API로 SHA-256 해시
2. 코드에 저장된 해시와 비교
3. 일치하면 `sessionStorage`에 인증 플래그 저장

장점:
- 평문 비밀번호를 HTML/JS에 직접 넣지 않음
- 일반 사용자에게 간단한 진입 확인 제공

한계:
- 저장소나 개발자도구를 볼 수 있는 사용자는 로직을 우회할 수 있음
- 기밀 데이터 보호용으로 사용하면 안 됨

진짜 인증이 필요하면 GitHub Pages가 아닌 Cloudflare Access, Netlify Identity/Functions, Firebase Auth, Supabase Auth 같은 서버/엣지 인증 계층이 필요하다.

---

## 9. 시각 디자인 시스템

### 색상

Light:
- Background: `#F4F7FA`
- Surface: `#FFFFFF`
- Primary text: `#14202B`
- Secondary text: `#657383`
- Accent: 청록/청색 계열
- Warning: amber
- Danger: red

Dark:
- Background: `#0B1118`
- Surface: `#121B24`
- Primary text: `#E7EDF3`
- Secondary text: `#91A0AF`

### 타이포그래피

외부 폰트에 의존하지 않고 시스템 UI 폰트를 사용한다.

`Inter`가 설치되어 있으면 우선 사용하고, 그 외에는 `Pretendard`, `Apple SD Gothic Neo`, `Noto Sans KR`, `Segoe UI`, sans-serif 순으로 fallback 한다.

### 정보 밀도

- 카드: 핵심 요약
- 표: 정밀 조회
- 상세 패널: 긴 설명

데스크톱에서는 좌측 260px 내비게이션, 모바일에서는 오버레이 메뉴로 전환한다.

---

## 10. 데이터 무결성 및 추적성

- 원본 파일 SHA-256을 데이터 메타데이터에 저장한다.
- 웹 데이터는 원본 Excel의 **계산된 셀 값 + 수식 문자열**을 함께 보존한다.
- 출처/주의 시트 전체 내용을 웹에서 조회 가능하다.
- 다운로드되는 CSV는 현재 화면의 필터 결과만 포함한다.

---

## 11. 성능 설계

원본 데이터 규모가 작기 때문에 서버 검색 엔진이나 가상 스크롤은 필요하지 않다.

- 데이터 약 127 KB를 단일 `data.js`로 로드
- 모든 필터는 브라우저 메모리에서 실행
- 차트는 Canvas API로 직접 렌더링
- 외부 CDN 없음

GitHub Pages에서 초기 로딩 후 즉시 작동한다.

---

## 12. 접근성 및 편의 기능

- `/` 키: 전체 검색 포커스
- `Esc`: 상세 패널/모바일 메뉴 닫기
- 테마 선택 저장
- 표 헤더 sticky
- 모바일 가로 스크롤
- 키보드 포커스 outline
- `prefers-reduced-motion` 존중
- CSV 내보내기
- 즐겨찾기
- URL hash 기반 화면 이동
- 표 빈 결과 안내

---

## 13. 파일 구조

```text
NatureNuclideLibrary/
├─ index.html
├─ README.md
├─ DESIGN.md
├─ .nojekyll
└─ assets/
   ├─ styles.css
   ├─ data.js
   └─ app.js
```

---

## 14. 배포 구조

GitHub 저장소 루트에 위 파일을 업로드한 후:

1. Repository Settings
2. Pages
3. Deploy from a branch
4. `main` / `/ (root)` 선택
5. 저장

앱은 상대경로만 사용하므로 프로젝트 페이지에서도 동작한다.

---

## 15. 검증 기준

### 기능
- 비밀번호 `redpass` 통과
- Light/Dark 저장
- 11개 시트 조회
- 감마 라인 88개 조회
- 에너지 검색 정확성
- 검출기 적합성 필터
- CSV 저장
- FWHM 계산
- 모바일 레이아웃

### 데이터
- 원본 감마 라인 수 = 88
- 고유 핵종 수 = 53
- 에너지 범위 = 46.539–6129 keV
- 원본 시트 수 = 11

### 주의
- `●◐✕`는 에너지창 적합성의 참고 판정이며 최종 MDA/동정 판정이 아님
- 간섭 계산은 이상화된 FWHM 기반 분리도 지표임
- 실제 검출기 교정값, 시료 기하, 자기흡수, 효율, 배경, 계수시간을 별도로 반영해야 함

---

## 16. 향후 확장

정적 사이트를 유지하면서 추가 가능한 기능:
- 사용자 CSV/JSON 핵종 라이브러리 로드
- detector calibration JSON import/export
- 사용자 정의 검출기 FWHM 계수 저장
- 피크 목록 일괄 업로드 후 후보 핵종 자동 매칭
- 여러 피크의 공통 핵종 점수화
- 핵종 붕괴사슬 그래프
- PWA 오프라인 설치
- 영문/국문 이중 언어
- URL query로 판독 세션 공유

서버 기능을 허용할 경우:
- 사용자 계정 및 권한
- 팀별 라이브러리 버전 관리
- 검출기 교정 이력
- 분석 결과 저장
- 감사 로그
